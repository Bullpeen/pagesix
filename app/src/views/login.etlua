<% if current_user then %>
	<p>Welcome, <%= current_user.user_name %>!</p>
<% else %>
	<h1>Log in</h1>
	<% if error then %><p data-alert="danger"><%= error %></p><% end %>
	<% render('views.fragments.login') %>
	<% render('views.fragments.oauth_buttons') %>
<% end %>
